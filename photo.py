
class Photo(object):

    def __init__(self, code, orientation, tags, number_of_tags):
        self.code = code
        self.orientation = orientation
        self.tags = tags
        self.number_of_tags = number_of_tags

